(ns io.github.clechasseur.adventofcode.y2023.input.d05)

(def input
  {:seeds [0] ;; <REDACTED>
   :maps [{:name :seed-to-soil
           :ranges [{:dest-range-start 0 :source-range-start 0 :range-size 0} ;; <REDACTED>
                    ]}
         ]})
